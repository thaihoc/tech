# Hướng dẫn ubuntu cơ bản

# Cài đặt lệnh dnsutil

```
apt-get update
apt-get install dnsutils -y
```

Sử dụng:

```
dig example.com
```

# Cài đặt lệnh ping

```
apt-get update
apt-get install iputils-ping
```

# Phân giải domain

nslookup example.com