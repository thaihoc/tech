# Hướng dẫn ubuntu cơ bản

# Cài đặt lệnh dnslookup

apt-get update
apt-get install dnsutils -y

# Cài đặt lệnh ping

apt-get update
apt-get install iputils-ping

# Phân giải domain

nslookup example.com